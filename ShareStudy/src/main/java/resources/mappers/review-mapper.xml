<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<entry key="selectReviewList">
	SELECT *
	FROM (SELECT ROWNUM RNUM,A.*
      	  FROM(SELECT REVIEW_NO,
                 	  REGION_NAME,
                      REVIEW_TITLE,
                      USER_ID,
                      REVIEW_COUNT,
                      REVIEW_DATE
               FROM REVIEW
               ORDER BY REVIEW_DATE DESC) A)
	WHERE RNUM BETWEEN ? AND ?
	</entry>
	
	<entry key="insertReview">
	INSERT INTO REVIEW(REVIEW_NO,
					   REVIEW_TITLE,
                   	   REGION_NAME,
                       REVIEW_CONTENT)
	VALUES(SEQ_RNO.NEXTVAL,?,?,?)
	</entry>
	
	
	<!-- 총 게시글 개수 조회 -->
	<entry key="listCount">
	SELECT COUNT(*) COUNT
	FROM REVIEW
	</entry>
	
	<entry key="insertAttachmentList">
	INSERT INTO REATTACHMENT(FILE_NO,
                             REVIEW_NO,
                             ORIGIN_NAME,
                             CHANGE_NAME,
                             FILE_PATH
                             FILE_LEVEL)
	VALUES(SEQ_FNO.NEXTVAL,?,?,?,?,?)
	</entry>
	
	<!-- 게시글 번호 추출(시퀀스 발행) -->
	<entry key="selectReviewNo">
		SELECT SEQ_RNO.NEXTVAL RNO
		FROM DUAL
	</entry>
	
	
	
	
	
	
	
	
</properties>
